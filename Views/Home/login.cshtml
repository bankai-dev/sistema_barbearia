<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <!-- Link para os arquivos CSS do Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Estilos personalizados para a página de login -->
    <style>
        /* Estilos específicos para o corpo da página */
        body {
            margin: 0;
            padding: 0;
            height: 90vh; /* Definindo a altura da tela como 100% da altura visível */
            background-image: url('caminho/para/sua/imagem.jpg');
            background-size: cover;
            background-repeat: no-repeat;
            background-position: center;
            display: flex;
            justify-content: center;
            align-items: center; /* Centralizando verticalmente */
        }

        /* Estilos para a caixa de login */
        .login-box {
            background-color: rgba(255, 255, 255, 0.8); /* Fundo branco transparente */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2); /* Sombra */
            text-align: center;
            
        }

        /* Estilos para os botões tornando-os vermelhos */
        .btn-danger {
            color: #fff;
            background-color: #dc3545;
            border-color: #dc3545;
            margin-left: 140px;
        }
    </style>
</head>
<body>
    <div class="login-box">
        <img src="caminho/para/sua/imagem-barbearia.jpg" alt="Barbearia Logo" class="mb-4" style="max-width: 200px;">
        <h1 class="mb-4">Login</h1>
        <!-- Formulário de login -->
        <form>
            <div class="mb-3">
                <input type="text" class="form-control mb-3" id="username" placeholder="Username">
                <input type="password" class="form-control mb-3" id="password" placeholder="Password">
                <!-- Botão de login com classe btn-danger -->
                <button type="submit" class="btn btn-danger btn-block">Login</button>
            </div>
        </form>
    </div>
</body>
</html>
